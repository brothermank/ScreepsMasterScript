/*
 * Module code goes here. Use 'module.exports' to export things:
 * module.exports.thing = 'a thing';
 *
 * You can import it from another modules like this:
 * var mod = require('intelligence.energy');
 * mod.thing == 'a thing'; // true
 */

var energyIntelligence = {
    
        
    
    
    
    spotsAt : function(structure){
        var positions = 0;
        var objectsAtPosition;
        objectsAtPosition = (new RoomPosition(structure.pos.x + 1, structure.pos.y)).look;
        
        
        if() positions++;
        
        structure.pos
    }
    
}

module.exports = energyIntelligence;